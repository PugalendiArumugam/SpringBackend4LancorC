spring.application.name=AppartmentEzy
server.port=8080

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# PostgreSQL (Matches your working setup)
spring.datasource.url=jdbc:postgresql://localhost:5432/lancorC
spring.datasource.username=postgres
spring.datasource.password=admin

# Email Configuration (Port 465 / SSL)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=pugal.a@gmail.com
spring.mail.password=lhbqksvoltkodabi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.jackson.property-naming-strategy=SNAKE_CASE

# JWT Configuration
app.jwt.secret=apartmentez-super-secret-jwt-key-change-this-in-production-2024
app.jwt.expiration-days=10

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# UUID handling for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect